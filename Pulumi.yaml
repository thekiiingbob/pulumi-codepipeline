name: pulumi_codepipeline
description: Example of using pulumi to configure AWS Codepipeline + Codebuild
runtime: nodejs
template:
  description: A minimal AWS JavaScript program
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
